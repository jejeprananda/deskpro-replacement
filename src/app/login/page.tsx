import { LoginForm } from "@/components/auth/login-form";

export default function LoginPage() {
  return (
    <div className="login-page flex min-h-dvh w-full flex-1 items-center justify-center px-4 py-12">
      <LoginForm />
    </div>
  );
}
